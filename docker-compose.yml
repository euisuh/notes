services:
  notes-nginx:
    build: .
    image: notes:latest
    container_name: notes
    restart: unless-stopped
    ports:
      - "127.0.0.1:8088:80"
    depends_on:
      - keyring-backend

  keyring-backend:
    build: ./backend
    image: notes-auth:latest
    container_name: keyring-backend
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      KEYRING_CREDENTIAL: ${KEYRING_CREDENTIAL:-you@email.com:changeme}
